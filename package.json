{
  "name": "placement-cell",
  "version": "1.0.0",
  "description": "Vidya Sethu - Training & Placement Cell Management System",
  "scripts": {
    "start": "node backend/server.js",
    "dev": "cd backend && nodemon server.js",
    "install:backend": "cd backend && npm install",
    "postinstall": "cd backend && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
